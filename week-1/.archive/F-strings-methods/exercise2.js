const name = " Daniel  ";

function number_letters(name) {

    let i = 0;
    for (i; i < name.trim().length; i++) {
        const element = name[i];
    }
    return (i);
}

const i = number_letters(name);

let message = ("My name is " + name.trim() + " and my name is " + i + " characters long")

console.log(message);